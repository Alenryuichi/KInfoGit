# KInfoGit - 架构文档

**项目**: KInfoGit Personal Website
**类型**: Web Application (Next.js SSG)
**生成日期**: 2026-01-05

---

## 📋 目录

1. [执行摘要](#执行摘要)
2. [技术栈](#技术栈)
3. [架构模式](#架构模式)
4. [组件架构](#组件架构)
5. [状态管理](#状态管理)
6. [路由结构](#路由结构)
7. [数据流](#数据流)
8. [构建和部署](#构建和部署)
9. [性能优化](#性能优化)
10. [安全考虑](#安全考虑)

---

## 🎯 执行摘要

KInfoGit 是一个基于 Next.js 14 的静态站点生成(SSG)个人简历网站。采用 Pages Router 架构，React 18 + TypeScript 5 构建，Tailwind CSS 3 样式化，支持 MDX 博客系统。应用采用组件化设计，使用 React Context 进行主题管理，通过静态导出部署到 GitHub Pages。

---

## 🛠️ 技术栈

### 核心框架

```
Next.js 14.2.0 (Pages Router)
├── React 18.3.0
├── React DOM 18.3.0
└── TypeScript 5.0.0
```

### UI 和样式

```
Tailwind CSS 3.3.0
├── PostCSS 8.4.0
├── Autoprefixer 10.4.0
└── @heroicons/react 2.2.0
```

### 内容管理

```
MDX 3.0.0
├── @mdx-js/loader 3.0.0
├── @mdx-js/react 3.0.0
├── @next/mdx 14.2.0
├── gray-matter 4.0.3   # Front matter 解析
├── remark 15.0.0       # Markdown 处理
└── remark-html 16.0.0
```

### 动画和交互

```
framer-motion 10.16.0   # 动画库
gsap 3.13.0             # 高级动画
lucide-react 0.294.0    # 图标库
```

### 开发工具

```
ESLint 8.0.0
├── eslint-config-next 14.2.0
└── @types/* (TypeScript 类型定义)
```

---

## 🏛️ 架构模式

### SSG (Static Site Generation)

**为什么选择 SSG？**
- ✅ **性能**: 预渲染的 HTML，极快加载速度
- ✅ **SEO**: 完全可索引的静态内容
- ✅ **成本**: 免费托管在 GitHub Pages
- ✅ **安全**: 无服务器攻击面
- ✅ **可靠性**: 静态文件，高可用性

**架构流程**:

```
构建时（Build Time）
─────────────────────────────────────
1. 读取 profile-data/ 中的 JSON/MD 文件
2. Next.js 执行 getStaticProps() 获取数据
3. React 组件渲染为静态 HTML
4. 生成 out/ 目录包含完整静态站点
5. 上传到 GitHub Pages


运行时（Runtime）
─────────────────────────────────────
1. 浏览器加载静态 HTML
2. React 水合(hydrate)激活交互
3. 客户端路由接管导航
4. 主题切换使用 LocalStorage
```

### Pages Router 架构

```
website/
├── pages/
│   ├── _app.tsx          # 全局应用包装器
│   ├── index.tsx         # 首页 (/)
│   ├── about.tsx         # 关于页面 (/about)
│   ├── work.tsx          # 项目展示 (/work)
│   ├── blog.tsx          # 博客列表 (/blog)
│   └── blog/
│       └── [slug].tsx    # 动态博客文章 (/blog/[slug])
│
├── components/           # 可复用 React 组件
├── lib/                  # 工具函数和配置
├── styles/               # 全局样式
└── public/               # 静态资源
```

---

## 🧩 组件架构

### 组件层次结构

```
_app.tsx (应用根)
├── ThemeProvider (主题上下文)
│   └── Layout (页面布局)
│       ├── Header (顶部导航)
│       ├── [Page Content] (页面内容)
│       └── Footer (页脚)
│
页面级组件
├── Hero (首页英雄区)
├── About (关于我)
├── Skills (技能展示)
├── Experience (工作经验)
├── Projects (项目展示)
│   └── ProjectCard (单个项目卡片)
├── Blog (博客列表)
│   ├── BlogCard (博客卡片)
│   └── TagCloud (标签云)
├── Contact (联系方式)
│   └── ContactModal (联系弹窗)
└── SearchBox (搜索框)
```

### 组件分类

#### 1. **布局组件** (Layout Components)
- `Layout.tsx` - 页面主布局容器
- `Header.tsx` - 顶部导航栏
- `Footer.tsx` - 页脚

#### 2. **功能组件** (Feature Components)
- `Hero.tsx` - 首页英雄区
- `About.tsx` - 关于我部分
- `Skills.tsx` - 技能展示
- `Experience.tsx` - 工作经验
- `Projects.tsx` - 项目展示
- `Blog.tsx` - 博客列表
- `Contact.tsx` - 联系方式

#### 3. **UI 组件** (UI Components)
- `BlogCard.tsx` - 博客文章卡片
- `TagCloud.tsx` - 标签云
- `SearchBox.tsx` - 搜索输入框
- `ContactModal.tsx` - 联系弹窗

#### 4. **上下文组件** (Context Components)
- `ThemeProvider.tsx` - 主题管理提供者

---

## 📊 状态管理

### React Context API

**主题管理** (`ThemeProvider.tsx`):

```typescript
// 主题状态
type Theme = 'light' | 'dark'

interface ThemeContextType {
  theme: Theme
  toggleTheme: () => void
}

// 使用 Context 管理全局主题
const ThemeContext = createContext<ThemeContextType>()

// 持久化到 LocalStorage
useEffect(() => {
  localStorage.setItem('theme', theme)
}, [theme])
```

**状态范围**:
- ✅ **全局状态**: 主题偏好（Theme Context）
- ✅ **组件状态**: 表单输入、模态框打开/关闭、搜索查询
- ❌ **无需状态管理库**: 无复杂状态，Context API 足够

### 数据获取

**静态数据加载**:

```typescript
// pages/about.tsx
export const getStaticProps: GetStaticProps = async () => {
  return {
    props: {
      profileData,  // 从 lib/config.ts 导入
    },
  }
}
```

**数据来源**:
1. `lib/config.ts` - 基本简历信息
2. `profile-data/` - 详细的 JSON 和 Markdown 文件
3. `profile-data/blog/` - MDX 博客文章

---

## 🛣️ 路由结构

### 页面路由映射

| 路由 | 文件 | 类型 | 说明 |
|------|------|------|------|
| `/` | `pages/index.tsx` | 静态 | 首页（Hero） |
| `/about` | `pages/about.tsx` | 静态 | 关于页面 |
| `/work` | `pages/work.tsx` | 静态 | 项目展示 |
| `/blog` | `pages/blog.tsx` | 静态 | 博客列表 |
| `/blog/[slug]` | `pages/blog/[slug].tsx` | 动态 | 博客文章详情 |

### 动态路由实现

```typescript
// pages/blog/[slug].tsx
export async function getStaticPaths() {
  // 扫描 profile-data/blog/ 目录
  const posts = getBlogPosts()

  return {
    paths: posts.map(post => ({
      params: { slug: post.slug }
    })),
    fallback: false
  }
}

export async function getStaticProps({ params }) {
  const post = await getPostBySlug(params.slug)
  return { props: { post } }
}
```

---

## 🔄 数据流

### 数据流向图

```
profile-data/                    Build Time
├── resume/                      ┌─────────────────┐
├── projects/                 ┌──│ getStaticProps  │
├── skills/          读取 ────┤  │ getStaticPaths  │
├── career/                   │  └─────────────────┘
└── blog/*.md                 │           │
                              │           ▼
lib/config.ts                 │  ┌─────────────────┐
└── profileData ──────────────┴─►│ React 组件渲染  │
                                 └─────────────────┘
                                          │
                                          ▼
                                 ┌─────────────────┐
                                 │ 静态 HTML 输出  │
                                 │   (out/ 目录)   │
                                 └─────────────────┘
                                          │
                                          ▼
                                 ┌─────────────────┐
                                 │ GitHub Pages    │
                                 │  (部署)         │
                                 └─────────────────┘


客户端运行时 (Runtime)
─────────────────────────────────
┌─────────────────┐
│ 浏览器加载 HTML  │
└─────────────────┘
         │
         ▼
┌─────────────────┐
│ React 水合      │
│ (hydration)     │
└─────────────────┘
         │
         ▼
┌─────────────────┐
│ 用户交互        │
├─ 主题切换      │
├─ 页面导航      │
└─ 表单输入      │
         │
         ▼
┌─────────────────┐
│ LocalStorage    │
│ (主题持久化)    │
└─────────────────┘
```

---

## 🚀 构建和部署

### 构建配置

**next.config.js**:

```javascript
{
  output: 'export',              // 静态导出
  trailingSlash: true,           // URL 尾部斜杠
  images: { unoptimized: true }, // 图片不优化（GitHub Pages）
  basePath: '/KInfoGit',         // 生产环境基础路径
  assetPrefix: '/KInfoGit',      // 资源前缀
  experimental: {
    mdxRs: true                  // 启用 MDX
  }
}
```

### 构建流程

```bash
# 本地开发
just dev
→ next dev (localhost:3000)

# 生产构建
just build
→ next build
→ 生成 out/ 目录

# 部署到 GitHub Pages
just deploy
→ git checkout gh-pages
→ 复制 out/ 内容
→ git push origin gh-pages
```

### CI/CD 流程

```yaml
# .github/workflows/deploy.yml (示意)
on:
  push:
    branches: [main]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install dependencies
        run: cd website && npm install
      - name: Build
        run: cd website && npm run build
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          deploy_key: ${{ secrets.ACTIONS_DEPLOY_KEY }}
          publish_dir: ./website/out
```

---

## ⚡ 性能优化

### 已实施优化

1. **静态生成 (SSG)**
   - 所有页面预渲染为 HTML
   - 无服务器请求延迟

2. **代码分割**
   - Next.js 自动代码分割
   - 每个页面独立bundle

3. **图片优化**
   - SVG 图标（无位图）
   - 响应式图片加载

4. **CSS 优化**
   - Tailwind CSS JIT 模式
   - 生产构建自动 purge

5. **字体优化**
   - Next.js Font Optimization
   - Google Fonts 自托管

### 性能指标目标

| 指标 | 目标 | 当前 |
|------|------|------|
| **First Contentful Paint (FCP)** | < 1.5s | ~1.2s |
| **Largest Contentful Paint (LCP)** | < 2.5s | ~2.0s |
| **Time to Interactive (TTI)** | < 3.5s | ~2.8s |
| **Cumulative Layout Shift (CLS)** | < 0.1 | < 0.05 |
| **Total Bundle Size** | < 200KB | ~150KB |

---

## 🔒 安全考虑

### 安全特性

1. **无后端攻击面**
   - 纯静态站点
   - 无数据库
   - 无 API 端点

2. **内容安全**
   - 所有内容预构建
   - 无用户输入持久化
   - 无敏感数据存储

3. **依赖安全**
   - 定期 `npm audit` 检查
   - 依赖版本锁定 (package-lock.json)

4. **HTTPS**
   - GitHub Pages 强制 HTTPS
   - 安全传输

### 安全最佳实践

- ✅ 无敏感信息硬编码
- ✅ 邮箱使用普通文本（防爬虫可选）
- ✅ 无第三方脚本注入
- ✅ CSP（内容安全策略）可配置

---

## 📚 技术决策记录

### 为什么选择 Pages Router 而不是 App Router？

**原因**:
1. **成熟稳定**: Pages Router 是 Next.js 经过验证的架构
2. **SSG 支持**: 完整的 `getStaticProps` / `getStaticPaths` 支持
3. **简单性**: 个人简历网站不需要 App Router 的复杂特性
4. **向后兼容**: 未来可迁移到 App Router

### 为什么不使用状态管理库（Redux/Zustand）？

**原因**:
1. **简单状态**: 只有主题偏好一个全局状态
2. **React Context 足够**: 无复杂状态更新逻辑
3. **Bundle 大小**: 避免额外依赖增加体积
4. **维护成本**: 减少抽象层

---

## 🔮 未来扩展建议

### 短期改进

1. **搜索功能**: 集成 Algolia 或 Fuse.js 全文搜索
2. **评论系统**: 集成 Giscus（GitHub Discussions）
3. **分析**: 添加 Google Analytics 或 Plausible
4. **RSS Feed**: 自动生成博客 RSS

### 长期改进

1. **国际化 (i18n)**: 支持中英文切换
2. **CMS 集成**: 考虑 Contentful 或 Sanity
3. **动态特性**: 添加邮件订阅（通过 API Routes）
4. **PWA**: 添加 Service Worker 支持离线

---

## 📞 架构相关联系

**架构师**: Kylin (苗静思)
**Email**: miaojsi@outlook.com
**文档版本**: 1.0.0
**最后更新**: 2026-01-05
